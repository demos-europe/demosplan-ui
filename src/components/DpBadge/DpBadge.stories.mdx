import { Meta } from '@storybook/addon-docs'
import DpBadge from './'

<Meta
  title="Components/Badge"
  component={DpBadge} />

# Badge

The Badge component renders a pill-like, informative item with an appropriate
background color, based on the information to be conveyed. It is typically used
to indicate state or progress of items in table rows.

## Default usage

```html
<dp-badge text="info text" />
```
