import { Canvas, Meta } from '@storybook/blocks'
import DpBadge from './DpBadge'

<Meta
  title="Components/DpBadge"
  component={DpBadge} />

# DpBadge

The DpBadge component renders an informative box with an appropriate background,
based on the information to be conveyed.

## Default usage

```html
<dp-badge text="info text" />
```