import { Canvas, Meta, Story } from '@storybook/blocks'
import * as DpButtonStories from './DpButton.stories.tsx'

import { action } from '@storybook/addon-actions'
import DpButton from './'

<Meta of={DpButtonStories} />

# Button

The `DpButton` component renders either a `<button>` or an `<a>` element, that has a visual `busy` state
which prevents the user from clicking twice and informs her something is happening behind the scenes.

## Colors

### Primary
<Canvas of={DpButtonStories.Primary} />

### Secondary
<Canvas of={DpButtonStories.Secondary} />

### Warning
<Canvas of={DpButtonStories.Warning} />

## Variants

### Solid
<Canvas of={DpButtonStories.Solid} />

### Outline
<Canvas of={DpButtonStories.Outline} />

### Subtle
<Canvas of={DpButtonStories.Subtle} />

## Rounded
<Canvas of={DpButtonStories.Rounded} />

## Render a link element
<Canvas of={DpButtonStories.LinkElement} />

## Use the 'busy' prop to visualize system status
<Canvas of={DpButtonStories.BusyState} />

## With Icon
<Canvas of={DpButtonStories.WithIcon} />

## Only Icon
<Canvas of={DpButtonStories.OnlyIcon} />
