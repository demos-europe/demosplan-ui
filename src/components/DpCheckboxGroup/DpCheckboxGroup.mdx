import { Canvas, Meta } from '@storybook/blocks'
import * as DpCheckboxGroupStories from './DpCheckboxGroup.stories.tsx'

<Meta of={DpCheckboxGroupStories} />

# CheckboxGroup

The `CheckboxGroup` component renders an inputs group of type `checkbox`.

Use it for multiple options that the user can turn on or off.

## Default usage

<Canvas of={DpCheckboxGroupStories.Default} />

## Inline checkboxes

<Canvas of={DpCheckboxGroupStories.Inline} />

## With label

<Canvas of={DpCheckboxGroupStories.WithLabel} />
