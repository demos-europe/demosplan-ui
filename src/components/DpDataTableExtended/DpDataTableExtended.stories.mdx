import { Meta } from '@storybook/addon-docs'
import DpDataTableExtended from './'

<Meta
    title="Components/DataTableExtended"
    component={DpDataTableExtended}
/>

# Extended Data table

Renders a pre-configured DpDataTable instance, along with a sticky header where a search bar and a pager including
items-per-page are placed. Additional actions can be placed in the sticky footer.

As in DpDataTable, be aware of the data structure for header-fields and table-items. The Keys in table-items have to
match the field values of the header-fields.

## Default usage

```html
<dp-data-table-extended
  :header-fields="[{field, label},{field, label}]"
  :table-items="[{fieldName}, {fieldName2}]" />
```

### Using all options

```html
<dp-data-table-extended
  :header-fields="[{field, label},{field, label}]"
  is-selectable
  is-sortable
  :init-items-per-page="50"
  :items-per-page-options="[10, 50, 100, 200]"
  :table-items="[{fieldName}, {fieldName2}]"
  @items-selected="emitsSelectedItemIds"
  track-by="id">
  <template v-slot:footer>
    <!-- Stuff for the footer -->
  </template>
</dp-data-table-extended>
```
