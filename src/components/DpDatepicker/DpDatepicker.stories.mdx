import { Meta } from '@storybook/addon-docs'
import DpDatepicker from './'

<Meta
    title="Components/Datepicker"
    component={DpDatepicker}
/>

# Datepicker

The Datepicker component use `a11y-datepicker` component.

## Default usage

```html
<dp-datepicker
  id="datepicker_id" />
```
