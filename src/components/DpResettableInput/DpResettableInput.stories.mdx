import { Meta } from '@storybook/addon-docs'
import DpResettableInput from './'

<Meta
    title="Components/ResettableInput"
    component={DpResettableInput}
/>

# Resettable Input

An input field which comes with an icon to dismiss the content, if there is any.

Clicking the dismiss-"x" emits "reset".

To provide the interface for DpInput, all configuration attributes for the input can be passed via `inputAttributes`.
