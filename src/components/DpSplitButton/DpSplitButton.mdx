import { Meta } from '@storybook/blocks'

<Meta title="Components/SplitButton" />

# Split Button

**The `DpSplitButton` component is deprecated.**

Instead, show all available actions in `DpFlyout`.
