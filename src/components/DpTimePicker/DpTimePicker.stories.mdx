import { Meta } from '@storybook/addon-docs'
import DpTimePicker from './'

<Meta
    title="Components/TimePicker"
    component={DpTimePicker}
/>

# TimePicker

The TimePicker component allows users to enter a time either by typing or by selecting the hour and minutes from two select elements.

## Default usage

```html
<dp-time-picker
    :id="timePicker"
    v-model="planTime" />
```
