import { Meta } from '@storybook/addon-docs'
import DpTimePicker from './'

<Meta
    title="Components/TimePicker"
    component={DpTimePicker}
/>

# TimePicker

The TimePicker component allows the users to enter a time either by typing or by selecting a time from the calendar.

## Default usage

```html
<dp-time-picker
    :id="timePicker"
    v-model="planTime" />
```
