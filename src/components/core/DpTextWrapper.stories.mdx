import { Meta } from '@storybook/addon-docs'
import DpTextWrapper from './'

<Meta
    title="Components/TextWrapper"
    component={DpTextWrapper}
/>

# TextWrapper

The TextWrapper component used to format text within div tag.

The purpose of a TextWrapper component is to manage and manipulate text content within a specific context or layout.

## Default usage

```html
<dp-text-wrapper text="add Text hier"/>
```
