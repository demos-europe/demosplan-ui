import { Meta } from '@storybook/addon-docs'
import DpUploadFiles from './DpUploadFiles'

<Meta
    title="Components/UploadFiles"
    component={DpUploadFiles}
/>

# UploadFiles

The UploadFiles component can be upload a single file or multiple files.

## Custom usage

```html
<dp-upload-files
    allowed-file-types="all"
    :basic-auth="dplan.settings.basicAuth"
    :get-file-by-hash="hash => RoutingFunction('core_file', { hash: hash })"
    :tus-endpoint="dplan.paths.tusEndpoint" />
```
